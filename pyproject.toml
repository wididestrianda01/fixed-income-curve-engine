[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "curveengine"
version = "0.1.0"
description = "Multi-curve fixed-income term structure construction, pricing and interest-rate risk"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy>=2.0",
    "scipy>=1.14",
    "pandas>=2.2",
    "requests>=2.32",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-cov>=5.0",
    "hypothesis>=6.112",
    "QuantLib>=1.36",
    "ruff>=0.6",
    "mypy>=1.11",
    "pandas-stubs>=2.2",
    "types-requests>=2.32",
    "pre-commit>=3.8",
    "nbmake>=1.5",
]
notebooks = [
    "jupyter>=1.1",
    "matplotlib>=3.9",
]
app = [
    "streamlit>=1.38",
    "matplotlib>=3.9",
]

[tool.hatch.build.targets.wheel]
packages = ["src/curveengine"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "RUF", "NPY", "PD"]
ignore = []

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["E501"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
files = ["src", "tests"]

[[tool.mypy.overrides]]
module = ["QuantLib.*", "scipy.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "pytest"
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=curveengine --cov-report=term-missing --cov-fail-under=80 --strict-markers"
markers = [
    "slow: Monte Carlo convergence and other long-running tests",
]
