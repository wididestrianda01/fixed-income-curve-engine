# --- Local planning artifacts: never publish ---
.context/
.claude/
# Local agent/browser-session state (ruflo/ruvector): never publish.
.ruflo/
.ruvector/
docs/superpowers/
PLAN.md
.codegraph/
graphify-out/
**/graphify-out/

# --- Data ---
# The frozen snapshot is packaged under src/yieldcurve/data/ and ships in the
# wheel (small, public, dated market data that makes the repo runnable offline).
# Everything else stays local.
data/raw/
data/processed/
data/cache/
# Legacy checkout snapshot root from before the refresh CLI was removed;
# the frozen snapshot is packaged under src/yieldcurve/data/.
data/snapshots/
*.parquet

# --- Models / artifacts ---
models/*.pkl
*.joblib

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
# Scratch build output for packaging tests (Task 13); never committed.
dist-review/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# --- Notebooks ---
# Notebook OUTPUTS are committed deliberately (recruiter-facing walkthrough);
# nbmake re-executes them in CI so committed outputs cannot go stale.
# notebooks/src/*.py sources are the reviewable source of truth (HYGIENE-04)
# and are deliberately NOT ignored: new sources must stay visible to git.
.ipynb_checkpoints/

# --- Secrets ---
.env
.env.*
*.pem
kaggle.json

# --- OS / editor ---
.DS_Store
.idea/
.vscode/
.serena/
.superpowers/sdd/
.worktrees/
.hypothesis/
.claude-flow/
.swarm/
ruvector.db
